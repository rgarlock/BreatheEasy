@model BreatheEasyApp.Models.ViewModels.GoalViewModel

@if(Model.AmountRemaining.HasValue)
 {
     @Html.DisplayFor(model => model.GoalItem)<text>:</text><br/>

        @Html.DisplayFor(model => model.AmountRemaining)
            
 }